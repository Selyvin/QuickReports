# Task Checklist Guidelines

- Keep the phase headings in `docs/tasks.md` intact.
- Mark completed tasks as `[x]` and unfinished tasks as `[ ]`.
- Add new tasks under the most appropriate existing phase whenever scope expands.
- Ensure every new or modified task references both its related plan item and requirement IDs.
- Preserve the existing numbered checklist format and indentation style for consistency.